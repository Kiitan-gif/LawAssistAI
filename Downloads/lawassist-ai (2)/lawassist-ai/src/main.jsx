import React from "react";
import ReactDOM from "react-dom/client";
import LawAssistApp from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <LawAssistApp />
  </React.StrictMode>
);
